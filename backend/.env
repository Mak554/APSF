# ─── Local Mode (no Firebase needed) ───────────────────────────────────────
# Set to false and provide serviceAccountKey.json to use real Firebase
USE_LOCAL_DB=false

# Firebase/Firestore Configuration (only needed when USE_LOCAL_DB=false)
FIREBASE_CREDENTIALS_PATH=./serviceAccountKey.json

# SMTP Email Configuration (needed to actually send test phishing emails)
SMTP_HOST=mail.privateemail.com
SMTP_PORT=587
SMTP_USER=security@apsf.site
SMTP_PASSWORD=Wshal3sha@

# Application Settings
APP_BASE_URL=http://localhost:8009
FRONTEND_URL=https://apsf.site
SECRET_KEY=apsf_dev_secret_key_change_in_production
